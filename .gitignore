bin/
obj/
*.user
.vs/
.idea/

# Reference checkouts (QuestFlow etc.) — read for design, never built in, never published.
.cursor/

# Markdown stays local (working notes) — only the README and changelog are published.
*.md
!README.md
!CHANGELOG.md

# Third-party source kept locally for study only — never built, never shipped. See NOTICE.md.
reference/
